diff --git a/.agents/MISSION_ANCHOR.md b/.agents/MISSION_ANCHOR.md
index c726d47..4115895 100644
--- a/.agents/MISSION_ANCHOR.md
+++ b/.agents/MISSION_ANCHOR.md
@@ -1,28 +1,26 @@
 ## Mission Anchor
 
 **User intent:**
-維護並演進 antigravity-lazy-pack 作為 HectorCIW 的通用 Antigravity 懶人包與 harness 基礎設施。
+用新架構（scope-drift-detector、failure-classifier、harness-engineer v0.2、Autonomous Safety Policy）審查這套新架構本身，找出設計缺陷、內部矛盾、執行落差、遺漏邊界。
 
 **Allowed scope:**
-- `/Users/wujunyi/.gemini/antigravity/scratch/antigravity-lazy-pack/` 內所有檔案
-- `~/.gemini/config/skills/`（全域 skill 更新）
-- `~/.gemini/config/AGENTS.md`（全域規則更新）
-- `HectorCIW/antigravity-lazy-pack` GitHub repo
+- 讀取所有相關 skill 文件（scope-drift-detector、failure-classifier、harness-engineer、AGENTS.md）
+- 讀取架構文件（autonomous_agent_architecture.md）
+- 只輸出審查報告，不修改任何 skill 文件
 
 **Out of scope:**
-- 不修改 `mathruffian-dot/` 或 `multica-ai/` 等外部 repo
-- 不動其他 GitHub repo（Agent_flexNIRS_Hector_v1、taiwan-street-eats、NIRS-DCS）除非明確指示
-- 不 push 任何 repo 除非明確確認
+- 不修改任何 skill 或 config 文件（除非審查後明確被要求）
+- 不 push 任何東西
 
 **Success condition:**
-專案 harness 完整（PROJECT_STATUS.md 填妥、AGENTS.md 存在、.agents/ 結構齊全），skills 均可正常觸發。
+產出一份結構化審查報告，涵蓋：設計缺陷、內部矛盾、執行落差、遺漏邊界、建議。
 
 **Stop condition:**
-涉及外部 repo 寫入、生產部署、或不確定的破壞性操作 → Tier 2 確認。
+如果發現需要修改超過 3 個 skill 文件 → Tier 2，先列出再確認。
 
 ---
-## Quick Drift Check（每 3 次 iteration 自查）
-- [ ] 還在 allowed scope 內？
-- [ ] 沒有猜測未驗證的套件或 API 名稱？
-- [ ] 同一操作失敗次數 < 2？
-- [ ] 沒有修改 > 3 個檔案（未確認的情況下）？
+## Quick Drift Check
+- [ ] 任務是「審查」，不是「修改」
+- [ ] 沒有猜測任何未驗證的資訊
+- [ ] 同一操作失敗次數 < 2
+- [ ] 沒有未經確認修改任何文件
